<%= provide(:title, 'Watch') %>
<div id="mainContent">
  <div id="videoSolo" data-video-id="<%= @video.id %>">
    <video id="videoPlayer" width="720" height="405" class="video-js vjs-default-skin active"
      controls preload="auto" data-setup='{}' poster="<%= @video.panda_video.encodings["thumbnail"].screenshots.first %>">
      <source src="<%= @video.panda_video.encodings["h264"].url %>" type="video/mp4" />
      <source src="<%= @video.panda_video.encodings["webm"].url %>" type="video/webm" />
    </video>
    <%= render 'videos/played' %>
    <%= render 'videos/solo_share' %>
    <%= render 'videos/solo_vote' if signed_in? %>
    <div class="videoLegend">
      <div class="video-name">
        <h1><%= @video.title %></h1>
      </div>
      <div class="video-info">
        <% if @video.user %>
            <%= link_to @video.user.name, @video.user, class: "user_name" %>
            <span class="user_location"><%= @video.user.city %>, <%= @video.user.state %></span>
        <% else %>
          <%= link_to "Shelter Me", "#", class: "user_name" %>
        <% end %>
      </div>
    </div>
    <div class="video-icons" style="float:right;margin-top:5px;">
      <ul>
        <li><%= render 'shared/icons/category', f: @video %></li>
        <li><%= render 'shared/icons/ranking', f: @video %></li>
        <li><%= render 'shared/icons/time', f: @video %></li>
        <li><%= render 'shared/icons/vote', f: @video %></li>
        <li><%= render 'shared/icons/play', f: @video %></li>
        <li><%= render 'shared/icons/share', f: @video %></li>
      </ul>
    </div>
  </div>
  <div class="clearfix"></div>
  <br><br>
  <div class="staticContent">
	<div class="votingControls"><% link_to "", "/videos/234" %></div>
    <p>Promote your favorite videos and help them win a prize by watching, sharing and voting for them. Mouse over a video to access controls for sharing and voting. The top rated videos as <%= link_to "determined by views, shares and votes", "/rules#semifinalists" %> will be eligible to win a prize.</p>
	<p>You do not need to create an account to watch or share a video, but you must be 
		<% if signed_in? %>
		signed in
		<% else %>
		<%= link_to "signed in", signin_path %> 
		<% end %>
		to vote for a video. You may vote for a video once every 24 hours.</p>
	<p>Views and shares were reset to ensure fairness during the voting period. Good luck!</p>
  </div>
</div>
<div class="clearfix"></div>